example-of-cython-pyinstaller-and-subzero
=========================================

This is an example project for
packaging Cython code with PyInstaller using subzero.

Example building command: `python setup.py build_exe`.
